{% extends "includes/layout_front.njk" %}
    
    {% block content %}
        <section class="destaque">
            <div class="destaque-texto">
                <h1>As melhores receitas</h1>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
            </div>
            <div class="destaque-boneco">
                <img src="/images/chef.png" alt="Chefe">
            </div>
        </section>
        <section class="mais-acessados">
            <h2>Mais acessadas</h2>
            <div class="cards-content">
                {% for recipe in recipes %}
                    <div class="card">
                        <a href="/recipes/{{recipe.id}}" alt="{{recipe.title}}" title="{{recipe.title}}" style="text-decoration:none">
                            <div class="card-image-container">
                                <img src="{{recipe.img}}" alt="{{recipe.title}}">
                            </div>
                            <div class="card-content">
                                <h3>{{recipe.title}}</h3>
                                <p>por {{recipe.author}}</p>
                            </div>
                        </a>
                    </div>
                {% endfor %}
            </div>
        </section>
    {%endblock%}
</body>
</html>